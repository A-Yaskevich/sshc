#!/usr/bin/env bash
set -euo pipefail

if [[ -n "${SSH_MOCK_LOG:-}" ]]; then
  printf 'sshpass %s\n' "$*" >>"$SSH_MOCK_LOG"
fi

if [[ "${1:-}" == "-e" ]]; then
  shift
fi

"$@"
exit "${SSHPASS_MOCK_EXIT:-0}"
